import { Game } from '../src/ts/game';

describe('Game', () => {
    let game;

    beforeEach(() => {
        game = new Game();
    })

    it('should start the game', () => {

    });
});